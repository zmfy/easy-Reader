# 复制本文件为 .env 并按需修改
# docker-compose 会自动读取根目录的 .env 文件

# ── 必填 ──────────────────────────────────────────
# JWT 密钥，生产环境请替换为随机字符串
JWT_SECRET=change-this-to-a-secure-random-string

# ── 可选 ──────────────────────────────────────────
# 服务端口（容器内部端口，对外映射见 docker-compose.yml）
PORT=3000

# 书库目录（容器内路径，宿主机路径在 docker-compose.yml volumes 中配置）
BOOKS_DIR=/app/books

# 数据库目录（容器内路径）
DATA_DIR=/app/data
